#!/usr/bin/env sh
set -eu

if [ "${ASO_SKIP_README_HOOK:-}" = "1" ]; then
  exit 0
fi

# Regenerate README from current workspace state before commit.
node scripts/update-readme.mjs --mode pre-commit
git -c safe.directory=* add README.md
