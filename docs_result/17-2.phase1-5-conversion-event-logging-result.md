# Phase-1.5 Conversion Reinforcement + Server-side Event Logging 구현 결과

> 작성일: 2026-03-02
> 대상 패키지: `apps/web`, `apps/api`, `packages/shared`
> 테스트: 12/12 통과 | 빌드: 6개 정적 라우트

---

## 1. 작업 범위 요약

| 파트 | 내용 |
|---|---|
| **Part A** | 전환 심리 강화: 랜딩 benchmark 카피, try 페이지 info banner, result GAP 인디케이터 |
| **Part B** | 서버 사이드 이벤트 수집: D1 `events` 테이블, `/api/events` 라우트, logger.ts API 전환 |
| **Part C** | 퍼널 집중: 추적 이벤트 5개로 축소 |

---

## 2. Part A — Conversion Reinforcement (Web)

### 2.1 랜딩 페이지 (`apps/web/src/app/page.tsx`)

**변경 1: remaining 힌트 제거**

랜딩에서 "남은 횟수" 표시를 완전히 제거했습니다. 랜딩은 유입 전환에 집중합니다.

- `remaining` 변수 삭제
- remaining hint 블록 삭제 (조건부 렌더링 전체)
- `useEffect` (page_view 로그) 삭제 → `useEffect` import 제거

**변경 2: benchmark tension 카피 삽입**

서브헤드라인 바로 아래에 비교 기준 문구 추가:

```tsx
<p style={{ fontSize: 14, color: '#6b7280', margin: '0 auto 28px' }}>
  {t.benchmark}
</p>
```

copy 객체에 추가된 `benchmark` 필드:

```typescript
const copy = {
  en: {
    // ...기존 필드...
    benchmark: 'Most apps score below 65. See where yours stands.',
  },
  ko: {
    // ...기존 필드...
    benchmark: '대부분의 앱은 65점 이하입니다. 내 앱은 몇 점일까요?',
  },
}
```

서브헤드라인의 하단 margin을 `36px → 16px`로 줄이고, benchmark 카피가 `28px` margin을 가져 전체 간격을 유지합니다.

**변경 3: page_view 이벤트 제거**

Phase 1.5에서 `page_view`는 추적 대상 이벤트가 아닙니다. `useEffect` 자체를 삭제하고 `useEffect` import도 제거했습니다.

---

### 2.2 /try 페이지 (`apps/web/src/app/try/page.tsx`)

**변경 1: info banner 추가**

폼 위에 중립적인 카테고리 평균 정보 배너 삽입:

```tsx
<div
  style={{
    marginBottom: 20,
    padding: '10px 14px',
    background: '#f8fafc',
    border: '1px solid #e2e8f0',
    borderRadius: 6,
    fontSize: 13,
    color: '#64748b',
    display: 'flex',
    gap: 16,
  }}
>
  <span>Recent category average score: <strong>62</strong></span>
  <span>Top-performing apps average <strong>78+</strong></span>
</div>
```

디자인 의도: 경고성이 아닌 중립적 회색조. 사용자가 자신의 앱 점수를 궁금하게 만드는 soft 앵커 역할.

**변경 2: untracked 이벤트 호출 제거**

- `logEvent('page_view', { page: 'try' })` + `useEffect` 삭제 → `useEffect` import 제거
- `logEvent('duplicate_request', { runId })` 삭제
- `logEvent('generate_error', ...)` 두 곳 모두 삭제

---

### 2.3 /result 페이지 (`apps/web/src/app/result/page.tsx`)

**변경 1: GAP 인디케이터**

score 숫자 아래, "/ 100 ASO Score" 아래에 즉시 렌더링:

```tsx
{result.benchmark_avg != null && (() => {
  const gap = result.score - result.benchmark_avg
  const positive = gap >= 0
  return (
    <p
      style={{
        margin: '10px 0 0',
        fontSize: 14,
        fontWeight: 600,
        color: positive ? '#16a34a' : '#dc2626',
      }}
    >
      {positive ? `+${gap}` : gap} vs category average ({result.benchmark_avg})
    </p>
  )
})()}
```

렌더링 예시:
- score=72, benchmark_avg=65 → `+7 vs category average (65)` (초록색)
- score=58, benchmark_avg=65 → `-7 vs category average (65)` (빨간색)
- benchmark_avg 없음 → 아무것도 렌더링하지 않음

`benchmark_avg`는 API 응답에서 localStorage에 저장된 값에서 읽습니다. 현재 API는 `65`를 하드코딩하여 반환합니다.

**변경 2: copy_variant 이벤트 제거**

```typescript
// 변경 전
const handleCopy = (variant, lines) => {
  navigator.clipboard.writeText(lines.join('\n')).then(() => {
    logEvent('copy_variant', { runId: runId ?? '', variant })  // 제거
    setCopied(variant)
    setTimeout(() => setCopied(null), 2000)
  })
}
```

---

## 3. Part B — Server-side Event Logging

### 3.1 D1 Migration (`apps/api/src/db/migrations/0003_events.sql`)

```sql
-- 0003_events.sql
-- Funnel event log. Additive-only migration.

CREATE TABLE IF NOT EXISTS events (
  event_id    TEXT PRIMARY KEY,
  event_name  TEXT NOT NULL,
  user_state  TEXT NOT NULL,
  route       TEXT NOT NULL,
  session_id  TEXT NOT NULL,
  payload_json TEXT,
  created_at  TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_events_created_at
  ON events(created_at DESC);
```

`payload_json`은 nullable (`TEXT`). 이벤트별 추가 메타데이터를 JSON 문자열로 저장합니다.

### 3.2 EventsRepo (`apps/api/src/repositories/events.repo.ts`)

```typescript
export class EventsRepo {
  constructor(private db: D1Database) {}

  async insert(params: {
    event_id: string
    event_name: string
    user_state: string
    route: string
    session_id: string
    payload_json: string | null
    created_at: string
  }): Promise<void> {
    await this.db
      .prepare(
        `INSERT INTO events
           (event_id, event_name, user_state, route, session_id, payload_json, created_at)
         VALUES (?, ?, ?, ?, ?, ?, ?)`,
      )
      .bind(
        params.event_id,
        params.event_name,
        params.user_state,
        params.route,
        params.session_id,
        params.payload_json,
        params.created_at,
      )
      .run()
  }
}
```

### 3.3 `/api/events` 라우트 (`apps/api/src/routes/events.ts`)

```typescript
import { Hono } from 'hono'
import type { AppEnv } from '../env'
import { EventsRepo } from '../repositories/events.repo'

const VALID_USER_STATES = ['anonymous', 'free', 'pro'] as const

const eventsRouter = new Hono<AppEnv>()

eventsRouter.post('/api/events', async (c) => {
  let body: unknown
  try {
    body = await c.req.json()
  } catch {
    return c.json({ ok: false, error: 'Invalid JSON' }, 400)
  }

  const b = body as Record<string, unknown>

  if (!b.event_name || typeof b.event_name !== 'string') {
    return c.json({ ok: false, error: 'event_name required' }, 400)
  }
  if (!b.route || typeof b.route !== 'string') {
    return c.json({ ok: false, error: 'route required' }, 400)
  }
  if (!b.session_id || typeof b.session_id !== 'string') {
    return c.json({ ok: false, error: 'session_id required' }, 400)
  }
  if (!b.user_state || !VALID_USER_STATES.includes(b.user_state as (typeof VALID_USER_STATES)[number])) {
    return c.json({ ok: false, error: 'user_state required' }, 400)
  }

  const repo = new EventsRepo(c.env.DB)
  await repo.insert({
    event_id: crypto.randomUUID(),
    event_name: b.event_name,
    user_state: b.user_state as string,
    route: b.route,
    session_id: b.session_id,
    payload_json: b.payload != null ? JSON.stringify(b.payload) : null,
    created_at: new Date().toISOString(),
  })

  return c.json({ ok: true })
})

export default eventsRouter
```

**검증 규칙**

| 필드 | 타입 | 검증 |
|---|---|---|
| `event_name` | string | required |
| `route` | string | required |
| `session_id` | string | required |
| `user_state` | string | required, `'anonymous' \| 'free' \| 'pro'` |
| `payload` | object | optional |

**`event_id`는 서버에서 생성** (`crypto.randomUUID()`). 클라이언트가 보내지 않습니다.

### 3.4 index.ts 라우트 등록

```typescript
import eventsRouter from './routes/events'
// ...
app.route('/', eventsRouter)
```

`/api/*` 경로는 기존 `uidCookieMiddleware`, `sessionCookieMiddleware`가 이미 적용되어 있어 추가 middleware 설정 불필요:

```typescript
app.use('/api/*', uidCookieMiddleware)
app.use('/api/*', sessionCookieMiddleware)
```

### 3.5 test/setup.ts 업데이트

```typescript
import migration0003 from '../src/db/migrations/0003_events.sql?raw'

beforeAll(async () => {
  // ...기존 0001, 0002...
  for (const stmt of runMigration(migration0003)) {
    await env.DB.prepare(stmt).run()
  }
})
```

### 3.6 `packages/shared/src/generate.ts` — `benchmark_avg` 추가

```typescript
export const GenerateResponseSchema = z.object({
  variants: z.object({ A: ..., B: ..., C: ... }),
  score: z.number(),
  breakdown: BreakdownSchema,
  recommendation: z.array(z.string()),
  benchmark_avg: z.number().optional(),   // ← 추가
})

export type GenerateResponse = z.infer<typeof GenerateResponseSchema>
// → benchmark_avg?: number
```

### 3.7 generate.ts Worker — `benchmark_avg: 65` 하드코딩

v2 경로와 v1 경로 모두 응답에 추가:

```typescript
const response: GenerateResponse = {
  variants: { ... },
  score: scoring.score,
  breakdown: scoring.breakdown,
  recommendation: scoring.recommendation,
  benchmark_avg: 65,   // ← 추가 (하드코딩, Phase 2에서 동적으로 교체 예정)
}
```

---

## 4. Part C — Updated Logger (`apps/web/src/lib/logger.ts`)

```typescript
const API_BASE = process.env.NEXT_PUBLIC_API_BASE ?? 'http://127.0.0.1:8787'

export type EventName =
  | 'generate_start'
  | 'generate_success'
  | 'limit_exceeded'
  | 'result_view'
  | 'upgrade_click'

function getSessionId(): string {
  try {
    let sid = sessionStorage.getItem('aso_session_id')
    if (!sid) {
      sid = crypto.randomUUID()
      sessionStorage.setItem('aso_session_id', sid)
    }
    return sid
  } catch {
    return 'unknown'
  }
}

export function logEvent(
  name: EventName,
  props?: Record<string, unknown>,
): void {
  if (typeof window === 'undefined') return

  const { user_state = 'anonymous', route = window.location.pathname, ...payload } =
    props ?? {}

  fetch(`${API_BASE}/api/events`, {
    method: 'POST',
    credentials: 'include',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      event_name: name,
      route,
      user_state,
      session_id: getSessionId(),
      payload: Object.keys(payload).length > 0 ? payload : undefined,
    }),
  }).catch(() => {
    // fire-and-forget: silently ignore failures
  })
}
```

**session_id 전략**

- `sessionStorage`에 `aso_session_id` 키로 UUID 저장
- 탭을 닫으면 세션 종료 (sessionStorage 특성)
- 탭 내에서는 동일한 session_id 유지 → 퍼널 분석 가능
- private browsing에서도 안전: `try/catch`로 fallback `'unknown'`

**fire-and-forget 패턴**

- `fetch().catch(() => {})` — 실패해도 UI 차단 없음
- `await` 없음 — 이벤트 전송이 메인 흐름을 블로킹하지 않음

**추적 이벤트 5개만 유지**

| 이벤트 | 발생 위치 | props |
|---|---|---|
| `generate_start` | 폼 제출 시 | `{runId, appName, category}` |
| `generate_success` | 200 응답 후 | `{runId, score}` |
| `limit_exceeded` | 429 응답 후 | `{runId, plan}` |
| `result_view` | /result 진입 시 | `{runId, score}` |
| `upgrade_click` | 업그레이드 버튼 | `{source}` |

**제거된 이벤트 (Phase 1.5 미추적)**

`page_view`, `generate_error`, `duplicate_request`, `copy_variant`

---

## 5. 테스트 및 빌드 결과

### API 테스트

```
✓ test/index.spec.ts (12 tests) 491ms
  Tests  12 passed (12)
  Duration 3.00s
```

migration 0003 추가 후에도 기존 12개 테스트 전원 통과.

### Web 빌드

```
✓ Compiled successfully in 1913.7ms

Route (app)
┌ ○ /
├ ○ /_not-found
├ ○ /pricing
├ ○ /result
├ ○ /success
└ ○ /try

○  (Static)  prerendered as static content
```

TypeScript 오류 없음. 6개 정적 라우트 생성.

---

## 6. 파일 변경 목록

### 신규 생성

| 파일 | 설명 |
|---|---|
| `apps/api/src/db/migrations/0003_events.sql` | events 테이블 + 인덱스 |
| `apps/api/src/repositories/events.repo.ts` | D1 insert 레포지토리 |
| `apps/api/src/routes/events.ts` | POST /api/events 라우트 |

### 수정

| 파일 | 변경 내용 |
|---|---|
| `packages/shared/src/generate.ts` | `GenerateResponseSchema`에 `benchmark_avg?: number` 추가 |
| `apps/api/src/routes/generate.ts` | v1/v2 양쪽 응답에 `benchmark_avg: 65` 추가 |
| `apps/api/src/index.ts` | `eventsRouter` import 및 `app.route` 등록 |
| `apps/api/test/setup.ts` | `migration0003` import 및 `beforeAll` 적용 |
| `apps/web/src/lib/logger.ts` | console.log → API fire-and-forget, EventName 5개로 축소, session_id 관리 |
| `apps/web/src/app/page.tsx` | benchmark 카피 추가, remaining 힌트 제거, page_view 이벤트 제거 |
| `apps/web/src/app/try/page.tsx` | info banner 추가, untracked 이벤트 호출 3건 제거 |
| `apps/web/src/app/result/page.tsx` | GAP 인디케이터 추가, copy_variant 이벤트 제거 |

---

## 7. 전환 흐름 변경 요약

### Before (Phase 1)

```
랜딩: 헤드라인 → 서브헤드 → CTA | 남은 횟수 표시
/try: 폼
/result: 점수 + 브레이크다운 + 변형
```

### After (Phase 1.5)

```
랜딩: 헤드라인 → 서브헤드 → "대부분 65점 이하" → CTA
/try: 카테고리 평균 62 / top 78+ 배너 → 폼
/result: 점수 → "+7 vs category average (65)" → 브레이크다운 + 변형
```

각 단계에서 사용자가 자신의 앱 점수를 궁금해하게 만드는 비교 앵커를 심었습니다. 이벤트는 D1에 서버 사이드로 수집되어 퍼널 분석이 가능합니다.
