# Phase-1 Web UX + Postsuccess Gate 패턴 구현 결과

> 작성일: 2026-03-02
> 대상 패키지: `apps/web`, `apps/api`
> 테스트: 12/12 통과

---

## 1. 작업 범위 요약

이번 세션에서 완료된 작업은 두 축으로 나뉩니다.

| 축 | 내용 |
|---|---|
| **API: Postsuccess Gate 패턴** | 사용자가 `usage-gate.service.ts`와 `generate.ts`를 직접 수정해 v2 게이트를 postsuccess 방식으로 재구성 |
| **Web: Phase-1 UX 구현** | 랜딩(`/`), 폼(`/try`), 결과(`/result`), 이벤트 로거, localStorage 결과 저장소, LimitExceededPrompt 플랜 분기 |

---

## 2. API: Postsuccess Gate 패턴

### 2.1 배경: 기존 v1 패턴의 문제

기존 v1 `checkAndGate`는 **생성 전** quota를 차감하는 방식이었습니다.

```
요청 → quota 차감 → 생성 실패 → quota 복구 불가 (손실 발생)
```

이를 해결하기 위해 **postsuccess 패턴**으로 재설계되었습니다.

### 2.2 Postsuccess 패턴 흐름

```
요청
 │
 ▼
startGateV2()
 ├─ idempotency 선점 (INSERT OR IGNORE)
 │   └─ 이미 존재 → 409 DUPLICATE_REQUEST
 ├─ 플랜 결정 (resolveUserPlan)
 ├─ Pro → 무제한 통과
 └─ 비Pro → quota 읽기 전용 체크
     └─ 초과 → 429 LIMIT_EXCEEDED
 │
 ▼
[AI 생성 실행]
 │
 ├─ 성공 → commitQuotaV2() → 원자적 증가
 │           └─ 증가 실패(경쟁) → markFailed() → 429 LIMIT_EXCEEDED
 │
 └─ 실패 → markFailed() → 에러 재던지기
 │
 ▼
markSucceeded() → idempotency 상태 = 'succeeded'
 │
 ▼
200 OK 응답
```

**핵심 보장**: quota는 생성이 실제로 성공한 후에만 차감됩니다.

### 2.3 `apps/api/src/services/usage-gate.service.ts` (전체)

```typescript
import { GenerationIdempotencyRepo } from '../repositories/generation-idempotency.repo'
import { UsageRepo } from '../repositories/usage.repo'
import { UserAuthProfilesRepo } from '../repositories/user-auth-profiles.repo'
import { EntitlementsRepo } from '../repositories/entitlements.repo'
import { getCurrentPeriod } from './entitlement.service'

const V2_ANON_LIFETIME_LIMIT = 2
const V2_MEMBER_MONTHLY_LIMIT = 5

export type UserPlan = 'anonymous' | 'free' | 'pro'

export type GateV2StartResult =
  | { ok: true; userPlan: UserPlan }
  | { ok: false; reason: 'duplicate' | 'limit_exceeded'; userPlan: UserPlan }

export async function startGateV2(
  db: D1Database,
  uid: string,
  idempotencyKey: string,
  isAuthenticated: boolean,
): Promise<GateV2StartResult> {
  const idempotencyRepo = new GenerationIdempotencyRepo(db)

  // 1. idempotency 선점
  const inserted = await idempotencyRepo.insert(uid, idempotencyKey)
  if (!inserted) {
    const userPlan = await resolveUserPlan(db, uid, isAuthenticated)
    return { ok: false, reason: 'duplicate', userPlan }
  }

  // 2. 플랜 결정
  const userPlan = await resolveUserPlan(db, uid, isAuthenticated)

  // 3. Pro → 무제한
  if (userPlan === 'pro') {
    return { ok: true, userPlan }
  }

  // 4. 슬롯 체크 (읽기 전용, 차감 없음)
  const usageRepo = new UsageRepo(db)
  const { periodStart } = getCurrentPeriod()

  if (userPlan === 'anonymous') {
    const current = await usageRepo.getLifetime(uid)
    const used = current?.used_count ?? 0
    if (used >= V2_ANON_LIFETIME_LIMIT) {
      return { ok: false, reason: 'limit_exceeded', userPlan }
    }
  } else {
    const current = await usageRepo.getMonthly(uid, periodStart)
    const used = current?.used_count ?? 0
    if (used >= V2_MEMBER_MONTHLY_LIMIT) {
      return { ok: false, reason: 'limit_exceeded', userPlan }
    }
  }

  return { ok: true, userPlan }
}

export async function commitQuotaV2(
  db: D1Database,
  uid: string,
  userPlan: UserPlan,
): Promise<boolean> {
  if (userPlan === 'pro') return true

  const usageRepo = new UsageRepo(db)
  const { periodStart } = getCurrentPeriod()

  if (userPlan === 'anonymous') {
    return usageRepo.incrementLifetimeIfBelowLimit(uid, V2_ANON_LIFETIME_LIMIT)
  }

  return usageRepo.incrementIfBelowLimit(uid, periodStart, V2_MEMBER_MONTHLY_LIMIT)
}

export async function markSucceeded(
  db: D1Database,
  uid: string,
  idempotencyKey: string,
) {
  const repo = new GenerationIdempotencyRepo(db)
  await repo.updateStatus(uid, idempotencyKey, 'succeeded')
}

export async function markFailed(
  db: D1Database,
  uid: string,
  idempotencyKey: string,
) {
  const repo = new GenerationIdempotencyRepo(db)
  await repo.updateStatus(uid, idempotencyKey, 'failed')
}

async function resolveUserPlan(
  db: D1Database,
  uid: string,
  isAuthenticated: boolean,
): Promise<UserPlan> {
  const entRepo = new EntitlementsRepo(db)
  const ent = await entRepo.get(uid)
  if (ent?.plan_code === 'pro' && ent.status === 'active') {
    return 'pro'
  }

  if (isAuthenticated) {
    const profilesRepo = new UserAuthProfilesRepo(db)
    const profile = await profilesRepo.get(uid)
    if (profile?.user_type === 'member') return 'free'
  }

  return 'anonymous'
}
```

### 2.4 플랜별 quota 제한

| 플랜 | 타입 | 한도 |
|---|---|---|
| `anonymous` | 평생(lifetime) | 2회 |
| `free` | 월별(monthly) | 5회 |
| `pro` | 없음 | 무제한 |

### 2.5 `apps/api/src/routes/generate.ts` (전체)

```typescript
import { Hono } from 'hono'
import { GenerateRequestSchema, GenerateResponseSchema, type GenerateRequest, type GenerateResponse } from '@aso-copilot/shared'
import { scoreCopy } from '@aso-copilot/scoring'
import type { AppEnv } from '../env'
import {
  startGateV2,
  commitQuotaV2,
  markSucceeded,
  markFailed,
} from '../services/usage-gate.service'

const generateRouter = new Hono<AppEnv>()

generateRouter.post('/generate', async (c) => {
  const uid = c.get('uid')
  const requestId = c.get('requestId')
  const isV2 = c.env.USAGE_POLICY_V2_ENABLED === 'true'

  // --- Body 파싱 ---
  let body: unknown
  try {
    body = await c.req.json()
  } catch {
    return c.json({ ok: false, error: 'Invalid JSON' }, 400)
  }

  const parsed = GenerateRequestSchema.safeParse(body)
  if (!parsed.success) {
    return c.json({ ok: false, error: parsed.error.flatten() }, 400)
  }

  // --- v2 경로 ---
  if (isV2) {
    const idempotencyKey = c.req.header('x-request-id')
    if (!idempotencyKey) {
      return c.json({ ok: false, error: 'MISSING_IDEMPOTENCY_KEY' }, 400)
    }

    const sessionUid = c.get('sessionUid')
    const isAuthenticated = c.get('isAuthenticated')
    const effectiveUid = sessionUid ?? uid

    const gate = await startGateV2(c.env.DB, effectiveUid, idempotencyKey, isAuthenticated)

    if (!gate.ok) {
      if (gate.reason === 'duplicate') {
        return c.json({ ok: false, error: 'DUPLICATE_REQUEST' }, 409)
      }
      return c.json(
        { ok: false, error: 'LIMIT_EXCEEDED', plan: gate.userPlan, upgrade_url: '/pricing' },
        429,
      )
    }

    try {
      const request: GenerateRequest = parsed.data
      const scoring = scoreCopy(parsed.data)

      const response: GenerateResponse = {
        variants: {
          A: [`${request.appName} A1`, `${request.appName} A2`],
          B: [`${request.category} B1`, `${request.category} B2`],
          C: ['C1', 'C2'],
        },
        score: scoring.score,
        breakdown: scoring.breakdown,
        recommendation: scoring.recommendation,
      }

      GenerateResponseSchema.parse(response)

      // 성공 후 quota commit
      const committed = await commitQuotaV2(c.env.DB, effectiveUid, gate.userPlan)

      if (!committed) {
        await markFailed(c.env.DB, effectiveUid, idempotencyKey)
        return c.json(
          { ok: false, error: 'LIMIT_EXCEEDED', plan: gate.userPlan, upgrade_url: '/pricing' },
          429,
        )
      }

      await markSucceeded(c.env.DB, effectiveUid, idempotencyKey)
      return c.json(response)
    } catch (err) {
      await markFailed(c.env.DB, effectiveUid, idempotencyKey)
      throw err
    }
  }

  // --- v1 경로 (게이트 없음, 무제한 생성) ---
  const request: GenerateRequest = parsed.data
  const scoring = scoreCopy(parsed.data)

  const response: GenerateResponse = {
    variants: {
      A: [`${request.appName} A1`, `${request.appName} A2`],
      B: [`${request.category} B1`, `${request.category} B2`],
      C: ['C1', 'C2'],
    },
    score: scoring.score,
    breakdown: scoring.breakdown,
    recommendation: scoring.recommendation,
  }

  GenerateResponseSchema.parse(response)
  return c.json(response)
})

export default generateRouter
```

**v1 경로 특성**: `USAGE_POLICY_V2_ENABLED !== 'true'`일 때 quota 게이트, idempotency 체크 없이 바로 생성합니다.

---

## 3. API 테스트 수정

### 3.1 변경 배경

`usage-gate.service.ts` 재작성으로 v1 `checkAndGate`가 제거되었습니다. 기존 테스트 두 개가 더 이상 유효하지 않았습니다.

| 기존 테스트 (제거) | 신규 테스트 (교체) |
|---|---|
| `'generate returns 429 after free plan limit'` | `'v1: generate succeeds regardless of repeat calls (no v1 gate)'` |
| `'duplicate request id returns 409'` | `'v1: same request-id sent twice both return 200 (no v1 dedup)'` |

### 3.2 신규 v1 동작 검증 테스트

```typescript
// v1 모드: 반복 요청도 모두 200
it('v1: generate succeeds regardless of repeat calls (no v1 gate)', async () => {
  const makeReq = (requestId: string) =>
    new Request('http://localhost/generate', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'x-request-id': requestId,
      },
      body: JSON.stringify({
        appName: 'No-Gate Test',
        category: 'Productivity',
        screenshots: ['s1.png', 's2.png', 's3.png', 's4.png', 's5.png', 's6.png'],
      }),
    })

  for (const id of ['ng-1', 'ng-2', 'ng-3', 'ng-4']) {
    const ctx = createExecutionContext()
    const res = await app.fetch(makeReq(id), env, ctx)
    await waitOnExecutionContext(ctx)
    expect(res.status).toBe(200)
  }
})

// v1 모드: 동일 request-id 재사용도 200
it('v1: same request-id sent twice both return 200 (no v1 dedup)', async () => {
  const req = () =>
    new Request('http://localhost/generate', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'x-request-id': 'v1-dedup-check-id',
      },
      body: JSON.stringify({
        appName: 'V1 Dedup',
        category: 'Productivity',
        screenshots: ['s1.png', 's2.png', 's3.png', 's4.png', 's5.png', 's6.png'],
      }),
    })

  const ctx1 = createExecutionContext()
  const r1 = await app.fetch(req(), env, ctx1)
  await waitOnExecutionContext(ctx1)
  expect(r1.status).toBe(200)

  const ctx2 = createExecutionContext()
  const r2 = await app.fetch(req(), env, ctx2)
  await waitOnExecutionContext(ctx2)
  expect(r2.status).toBe(200)
})
```

### 3.3 전체 테스트 결과

```
✓ test/index.spec.ts (12 tests) 429ms
  Tests  12 passed (12)
  Duration 2.54s
```

### 3.4 테스트 목록 (12개)

| # | 테스트 이름 | 예상 결과 |
|---|---|---|
| 1 | health | 200, `{ok:true, service:'api'}` |
| 2 | generate success | 200, variants/score/recommendation |
| 3 | generate validation error | 400, fieldErrors.screenshots |
| 4 | v1: generate succeeds regardless of repeat calls (no v1 gate) | 4회 모두 200 |
| 5 | v1: same request-id sent twice both return 200 (no v1 dedup) | 2회 모두 200 |
| 6 | GET /v1/entitlements returns shape | 200, free plan, 3회 한도 |
| 7 | v2: missing x-request-id returns 400 MISSING_IDEMPOTENCY_KEY | 400 |
| 8 | v2: anonymous user gets 429 after 2 lifetime generations | 1,2번 200 → 3번 429 |
| 9 | v2: duplicate idempotency key returns 409 DUPLICATE_REQUEST | 1번 200 → 2번 409 |
| 10 | GET /api/me returns shape | 200, uid/authenticated/plan/usage |
| 11 | scoreCopy should not inflate numeric score from filename-only screenshots | numeric ≤ 5, score ≤ 30 |
| 12 | scoreCopy should lower score when category is mismatched | aligned > mismatched |

---

## 4. Web Phase-1 UX 구현

### 4.1 구현 범위

```
apps/web/src/
├── lib/
│   ├── logger.ts          ← NEW: 콘솔 이벤트 로거
│   └── result-store.ts    ← NEW: localStorage 결과 저장소
├── app/
│   ├── layout.tsx         ← 메타데이터 업데이트
│   ├── page.tsx           ← 랜딩 페이지 (KR/EN 토글)
│   ├── try/
│   │   └── page.tsx       ← NEW: 생성 폼 페이지
│   └── result/
│       └── page.tsx       ← NEW: 결과 조회 페이지
└── components/
    └── LimitExceededPrompt.tsx  ← 플랜 분기 추가
```

### 4.2 정적 export 제약과 결과 URL 설계

`next.config.ts`에 `output: "export"` 설정으로 완전한 정적 배포(Cloudflare Pages)입니다.

**문제**: `/result/[runId]` 동적 라우트는 `generateStaticParams`가 필요한데, runId는 런타임에 생성됩니다.

**해결**: 쿼리 파라미터 방식 사용
```
/result?id={runId}
```
- `useSearchParams().get('id')`로 runId 읽기
- `<Suspense>` 래퍼 필수 (Next.js 요구사항)

### 4.3 `apps/web/src/lib/logger.ts`

```typescript
/**
 * 최소 클라이언트 이벤트 로거.
 * Phase 1: 콘솔 출력만. Phase 2에서 분석 SDK로 교체 예정.
 */

export type EventName =
  | 'page_view'
  | 'generate_start'
  | 'generate_success'
  | 'generate_error'
  | 'limit_exceeded'
  | 'duplicate_request'
  | 'result_view'
  | 'copy_variant'
  | 'upgrade_click'

export function logEvent(name: EventName, props?: Record<string, unknown>): void {
  if (typeof window === 'undefined') return
  console.log(`[aso] ${name}`, { ts: Date.now(), ...props })
}
```

**이벤트 목록**

| 이벤트 | 발생 위치 | props |
|---|---|---|
| `page_view` | 랜딩, try 진입 시 | `{page}` |
| `generate_start` | 폼 제출 시 | `{runId, appName, category}` |
| `generate_success` | 200 응답 후 | `{runId, score}` |
| `generate_error` | 오류 응답 후 | `{runId, status, error}` |
| `limit_exceeded` | 429 응답 후 | `{runId, plan}` |
| `duplicate_request` | 409 응답 후 | `{runId}` |
| `result_view` | /result 진입 시 | `{runId, score}` |
| `copy_variant` | 변형 복사 시 | `{runId, variant}` |
| `upgrade_click` | 업그레이드 버튼 클릭 | `{source}` |

### 4.4 `apps/web/src/lib/result-store.ts`

```typescript
import type { GenerateResponse } from '@aso-copilot/shared'

const PREFIX = 'aso-result-'

export function saveResult(runId: string, data: GenerateResponse): void {
  try {
    localStorage.setItem(PREFIX + runId, JSON.stringify(data))
  } catch {
    // 용량 초과 또는 시크릿 브라우징 — 무시
  }
}

export function loadResult(runId: string): GenerateResponse | null {
  try {
    const raw = localStorage.getItem(PREFIX + runId)
    return raw ? (JSON.parse(raw) as GenerateResponse) : null
  } catch {
    return null
  }
}
```

**저장 키 형식**: `aso-result-{runId}` (UUID v4)

runId는 idempotency key와 동일한 값을 사용합니다. 생성 성공 후 `saveResult(runId, result)` 호출 → `/result?id={runId}`로 이동 → `loadResult(runId)`로 복원.

### 4.5 `apps/web/src/app/layout.tsx`

```typescript
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'ASO Copilot – App Store Optimization',
  description:
    'AI-powered ASO scoring, keyword variants, and optimization tips for your App Store listing.',
}

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

### 4.6 `apps/web/src/app/page.tsx` — 랜딩 페이지

**구조**

```
Nav
 ├── ASO Copilot [Pro 뱃지]
 ├── KR/EN 토글 버튼
 ├── Pricing 링크 (비Pro만)
 └── "Try now" CTA → /try

Hero
 ├── AI 뱃지
 ├── 헤드라인 (pre-line, 줄바꿈 유지)
 ├── 서브헤드
 ├── CTA 버튼 → /try
 └── 남은 횟수 힌트 (useEntitlements)

Features (3열 그리드)
 ├── ASO Score
 ├── Keyword Variants
 └── Recommendations

Pricing 비교
 ├── Free 카드
 └── Pro 카드 (업그레이드 링크)

Footer
```

**KR/EN 복사 객체**

```typescript
const copy = {
  en: {
    badge: 'AI-powered ASO',
    headline: 'Boost Your\nApp Store Rankings',
    subhead: 'Get an instant quality score, three keyword variant sets, ...',
    cta: 'Try for free →',
    ctaSub: 'No sign-up required',
    features: [...],
    planFree: 'Free plan',
    planPro: 'Pro plan',
    planFreeSub: '3 generations / month',
    planProSub: 'Unlimited generations',
    upgrade: 'Upgrade to Pro →',
    remaining: (n: number) => `${n} free attempt${n === 1 ? '' : 's'} remaining this month`,
    usedAll: "You've used all free attempts this month.",
  },
  ko: {
    badge: 'AI 기반 ASO',
    headline: '앱스토어 순위를\n높이세요',
    subhead: 'AI로 즉시 ASO 품질 점수, 세 가지 키워드 변형, ...',
    cta: '무료로 시작하기 →',
    ctaSub: '회원가입 불필요',
    features: [...],
    planFree: '무료 플랜',
    planPro: 'Pro 플랜',
    planFreeSub: '월 3회 생성',
    planProSub: '무제한 생성',
    upgrade: 'Pro로 업그레이드 →',
    remaining: (n: number) => `이번 달 남은 무료 시도: ${n}회`,
    usedAll: '이번 달 무료 시도를 모두 사용했습니다.',
  },
}
```

**남은 횟수 힌트 렌더링**

```tsx
{!entLoading && !isPro && entitlements && (
  <p style={{ color: remaining === 0 ? '#dc2626' : '#6b7280' }}>
    {remaining === 0 ? t.usedAll : t.remaining(remaining ?? 0)}
  </p>
)}
```

- Pro 사용자: 힌트 미노출
- 남은 횟수 0: 빨간색 경고 문구
- 남은 횟수 있음: 회색 안내 문구

### 4.7 `apps/web/src/app/try/page.tsx` — 생성 폼 페이지

**핵심 로직: 이중 제출 방지**

```typescript
const submittingRef = useRef(false)

const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault()

  // React Strict Mode / 빠른 클릭 방지
  if (submittingRef.current) return
  submittingRef.current = true

  setLoading(true)
  setError(null)
  setLimitExceeded(false)
  setLimitPlan(undefined)

  // 제출마다 새 UUID 생성 (idempotency key)
  const runId = crypto.randomUUID()
  logEvent('generate_start', { runId, appName, category })

  try {
    const res = await fetch(`${API_BASE}/generate`, {
      method: 'POST',
      credentials: 'include',
      headers: {
        'Content-Type': 'application/json',
        'x-request-id': runId,
      },
      body: JSON.stringify({ appName, category, screenshots }),
    })

    if (res.status === 429) {
      const data = await res.json()
      if (data.error === 'LIMIT_EXCEEDED') {
        logEvent('limit_exceeded', { runId, plan: data.plan })
        setLimitPlan(data.plan)
        setLimitExceeded(true)
        return
      }
      setError('Too many requests. Please try again later.')
      return
    }

    if (res.status === 409) {
      logEvent('duplicate_request', { runId })
      setError('Duplicate request detected. Please try again.')
      return
    }

    if (!res.ok) {
      const data = await res.json()
      const msg = typeof data.error === 'string' ? data.error : JSON.stringify(data.error)
      logEvent('generate_error', { runId, status: res.status, error: msg })
      setError(msg)
      return
    }

    const result = (await res.json()) as GenerateResponse
    logEvent('generate_success', { runId, score: result.score })
    saveResult(runId, result)
    router.push(`/result?id=${runId}`)
  } catch (err) {
    const msg = err instanceof Error ? err.message : 'Network error'
    logEvent('generate_error', { runId, error: msg })
    setError(msg)
  } finally {
    setLoading(false)
    submittingRef.current = false
  }
}
```

**이중 제출 방지 설계**

`useState` 대신 `useRef` 사용 이유:
- `useState`는 setState 호출 시 리렌더링 발생 → 경쟁 조건 가능
- `useRef`는 동기적으로 읽고 쓸 수 있어 React Strict Mode의 이중 실행에도 안전

**제출 버튼 비활성화 조건**

```tsx
<button
  type="submit"
  disabled={loading || (remaining === 0 && !isPro)}
>
```

- `loading === true`: 요청 진행 중
- `remaining === 0 && !isPro`: 무료 한도 소진

**폼 구조**

```
앱 이름 (text input, required)
카테고리 (text input, required)
스크린샷 6개 (text input ×6, required)
  └── URL 또는 설명 텍스트 입력

[Generate ASO Report 버튼]
[LimitExceededPrompt] ← 429 시 노출
[Error banner] ← 기타 오류 시 노출
```

**스크린샷 입력 안내**

```tsx
<p style={{ fontSize: 12, color: '#9ca3af' }}>
  Paste screenshot URLs or descriptive captions for each image.
</p>
```

### 4.8 `apps/web/src/app/result/page.tsx` — 결과 페이지

**구조**

```typescript
// Suspense 필수: useSearchParams()는 Suspense 경계 내에서만 사용 가능
export default function ResultPage() {
  return (
    <Suspense fallback={<main>Loading…</main>}>
      <ResultContent />
    </Suspense>
  )
}

function ResultContent() {
  const searchParams = useSearchParams()
  const runId = searchParams.get('id')
  const [result, setResult] = useState<GenerateResponse | null>(null)
  const [notFound, setNotFound] = useState(false)
  const [copied, setCopied] = useState<string | null>(null)

  useEffect(() => {
    if (!runId) { setNotFound(true); return }
    const data = loadResult(runId)
    if (!data) { setNotFound(true); return }
    setResult(data)
    logEvent('result_view', { runId, score: data.score })
  }, [runId])
  // ...
}
```

**점수 색상 매핑**

```typescript
function scoreColor(score: number): string {
  if (score >= 80) return '#16a34a'  // 초록
  if (score >= 60) return '#ca8a04'  // 노랑
  return '#dc2626'                   // 빨강
}
```

**Breakdown 차원 레이블**

```typescript
const DIMENSION_LABELS: Record<keyof Breakdown, string> = {
  cta: 'CTA Strength',
  benefit: 'Benefit Clarity',
  clarity: 'Message Clarity',
  numeric: 'Numeric Proof',
  emotion: 'Emotional Pull',
}
```

**바 차트 색상 (각 차원, 최대 20점)**

```typescript
background: val >= 16 ? '#16a34a' : val >= 12 ? '#ca8a04' : '#ef4444'
width: `${(val / 20) * 100}%`
```

**변형 복사 기능**

```typescript
const handleCopy = (variant: 'A' | 'B' | 'C', lines: string[]) => {
  navigator.clipboard.writeText(lines.join('\n')).then(() => {
    logEvent('copy_variant', { runId: runId ?? '', variant })
    setCopied(variant)
    setTimeout(() => setCopied(null), 2000)
  })
}
```

복사 후 2초간 "✓ Copied" 상태 표시.

**not-found 상태**

localStorage에 해당 runId 데이터가 없을 때 표시:
```
🔍
Result not found
Results are stored locally and may have been cleared.
[Generate report →]
```

### 4.9 `apps/web/src/components/LimitExceededPrompt.tsx` — 플랜 분기

```typescript
interface Props {
  upgradeUrl?: string
  onDismiss: () => void
  plan?: string  // 'anonymous' | 'free' | undefined (v1 폴백)
}

export default function LimitExceededPrompt({ upgradeUrl = '/pricing', onDismiss, plan }: Props) {
  const isAnonymous = plan === 'anonymous'

  const title = isAnonymous ? 'Free trial limit reached' : 'Monthly limit reached'
  const body = isAnonymous
    ? "You've used your 2 free lifetime generations. Sign up or upgrade to continue."
    : "You've used all 3 free generations this month. Upgrade to Pro for unlimited access."

  return (
    <div style={{ background: '#fff8e1', border: '1px solid #f59e0b', ... }}>
      <p style={{ fontWeight: 600, color: '#92400e' }}>{title}</p>
      <p style={{ fontSize: 14, color: '#78350f' }}>{body}</p>
      <div>
        <button onClick={() => router.push(upgradeUrl)}>Upgrade to Pro</button>
        <button onClick={onDismiss}>Maybe later</button>
      </div>
    </div>
  )
}
```

**플랜별 메시지**

| `plan` 값 | 제목 | 본문 |
|---|---|---|
| `'anonymous'` | Free trial limit reached | "You've used your 2 free lifetime generations. Sign up or upgrade to continue." |
| `'free'` / `undefined` | Monthly limit reached | "You've used all 3 free generations this month. Upgrade to Pro for unlimited access." |

---

## 5. 발생한 문제와 해결 방법

### 5.1 정적 export + 동적 라우트 충돌

**문제**: `output: "export"` 설정 시 `/result/[runId]` 동적 라우트는 빌드 시 `generateStaticParams`가 필요합니다. 그러나 runId는 런타임에 생성되어 빌드 시 알 수 없습니다.

**해결**: 쿼리 파라미터 방식으로 전환
```
/result?id={runId}  (동적 라우트 없이 정적 페이지로 동작)
```

추가로 `useSearchParams()` 사용 시 Next.js가 `<Suspense>` 경계를 요구하므로 `ResultPage`를 `Suspense`로 감쌌습니다.

### 5.2 TSX 문자열 내 아포스트로피 파싱 오류

**문제**: 작은따옴표로 감싼 문자열 안의 `'`가 파서 오류를 유발합니다.

```typescript
// 오류
remaining: "You've used all free attempts this month.",  // 단일 따옴표 문자열 내 아포스트로피

// 해결
usedAll: "You've used all free attempts this month.",    // 큰따옴표 사용
```

`LimitExceededPrompt.tsx`의 동일한 패턴도 함께 수정:
```typescript
// 수정 전
? 'You\'ve used your 2 free lifetime generations. Sign up or upgrade to continue.'
// 수정 후
? "You've used your 2 free lifetime generations. Sign up or upgrade to continue."
```

### 5.3 v1 테스트 실패 (백엔드 변경 후)

**문제**: 사용자가 `usage-gate.service.ts`를 재작성해 v1 `checkAndGate`를 제거했습니다. 이에 따라 기존 v1 테스트 2개가 실패:
- `'generate returns 429 after free plan limit'` — v1에 quota 게이트가 없으므로 429 미발생
- `'duplicate request id returns 409'` — v1에 dedup이 없으므로 409 미발생

**해결**: 테스트를 새 v1 동작(무제한 200)에 맞게 교체하여 12/12 통과 확인.

---

## 6. 빌드 결과

```
apps/web $ pnpm build

Route (app)                 Size
─────────────────────────────────
○ /                        2.1 kB
○ /_not-found              0.9 kB
○ /pricing                 1.4 kB
○ /result                  3.2 kB
○ /success                 0.8 kB
○ /try                     2.8 kB

○ (Static)  prerendered as static content
```

6개 정적 라우트 생성, 빌드 오류 없음.

---

## 7. 사용자 흐름

```
랜딩 (/)
 │
 └─→ /try (폼)
      │
      ├─ 제출 → POST /generate
      │    ├─ 200 → saveResult(runId, data) → /result?id={runId}
      │    ├─ 429 LIMIT_EXCEEDED → LimitExceededPrompt 노출
      │    ├─ 409 DUPLICATE_REQUEST → 에러 메시지 노출
      │    └─ 기타 오류 → 에러 배너 노출
      │
      └─→ /result?id={runId}
           ├─ loadResult(runId) 성공 → 점수/브레이크다운/변형 표시
           └─ loadResult(runId) 실패 → "Result not found" 상태
```

---

## 8. 파일 변경 목록

### 신규 생성

| 파일 | 설명 |
|---|---|
| `apps/web/src/lib/logger.ts` | 콘솔 이벤트 로거 |
| `apps/web/src/lib/result-store.ts` | localStorage 결과 저장소 |
| `apps/web/src/app/try/page.tsx` | 생성 폼 페이지 |
| `apps/web/src/app/result/page.tsx` | 결과 조회 페이지 |

### 수정

| 파일 | 변경 내용 |
|---|---|
| `apps/web/src/app/layout.tsx` | 메타데이터 제목/설명 업데이트 |
| `apps/web/src/app/page.tsx` | 랜딩 페이지로 전면 재작성 (KR/EN 토글, 엔타이틀먼트 힌트) |
| `apps/web/src/components/LimitExceededPrompt.tsx` | `plan` prop 추가, 플랜별 메시지 분기 |
| `apps/api/src/services/usage-gate.service.ts` | Postsuccess 패턴으로 전면 재작성 (사용자 수정) |
| `apps/api/src/routes/generate.ts` | v2 postsuccess 흐름, v1 무게이트 경로 (사용자 수정) |
| `apps/api/test/index.spec.ts` | v1 테스트 2개 교체, v2 테스트 4개 추가 |
