**파일 단위 변경 계획**
1. 생성: [packages/scoring/package.json](/C:/Users/Administrator/aso-copilot/packages/scoring/package.json) `@aso-copilot/scoring` 워크스페이스 패키지 메타데이터(`name`, `private`, `type`, `exports`)와 `@aso-copilot/shared` 의존성 추가.
2. 생성: [packages/scoring/tsconfig.json](/C:/Users/Administrator/aso-copilot/packages/scoring/tsconfig.json) ESM/Bundler 기반 최소 TS 설정(`strict`, `noEmit`, `include: src/**/*.ts`).
3. 생성: [packages/scoring/src/scoreCopy.ts](/C:/Users/Administrator/aso-copilot/packages/scoring/src/scoreCopy.ts) `scoreCopy(input: GenerateRequest)` 구현, 반환 타입 `{ score, breakdown, recommendation }` 정의, 규칙 기반 점수화 로직 구현.
4. 생성: [packages/scoring/src/index.ts](/C:/Users/Administrator/aso-copilot/packages/scoring/src/index.ts) `scoreCopy`와 관련 타입 재수출.
5. 수정: [apps/api/package.json](/C:/Users/Administrator/aso-copilot/apps/api/package.json) `dependencies`에 `"@aso-copilot/scoring": "workspace:*"` 추가.
6. 수정: [apps/api/src/index.ts](/C:/Users/Administrator/aso-copilot/apps/api/src/index.ts) `/generate`에서 `scoreCopy(parsed.data)` 호출, `GenerateResponse`의 `score`/`recommendation` 값을 scoring 결과로 대체.
7. 수정: [apps/api/test/index.spec.ts](/C:/Users/Administrator/aso-copilot/apps/api/test/index.spec.ts) `generate success` 케이스에서 scoring 기반 `score`/`recommendation` 반환 검증, 검증 실패 케이스 유지.
8. 자동 갱신: [pnpm-lock.yaml](/C:/Users/Administrator/aso-copilot/pnpm-lock.yaml) 루트 `pnpm install` 후 워크스페이스 링크/의존성 변경 반영.

```text
Project root: C:\Users\Administrator\aso-copilot

Goal:
- Create a new workspace package `packages/scoring`
- Implement `scoreCopy(input: GenerateRequest)` with rule-based scoring
- Wire scoring engine into `apps/api` `/generate` endpoint

Constraints:
1) Reuse `GenerateRequest` from `@aso-copilot/shared`
2) Keep current `/generate` request validation flow in API
3) Keep `GenerateResponse` contract unchanged unless absolutely required
4) Use deterministic rule-based logic only (no randomness)

Implement exactly these file changes:

[CREATE] packages/scoring/package.json
- name: "@aso-copilot/scoring"
- private: true
- type: "module"
- exports:
  - ".": "./src/index.ts"
- dependencies:
  - "@aso-copilot/shared": "workspace:*"

[CREATE] packages/scoring/tsconfig.json
- Minimal TS config for ESM/Bundler
- include `src/**/*.ts`
- strict true, noEmit true

[CREATE] packages/scoring/src/scoreCopy.ts
- Import `GenerateRequest` from `@aso-copilot/shared`
- Export types:
  - `ScoreBreakdown`:
    - cta: number
    - benefit: number
    - clarity: number
    - numeric: number
    - emotion: number
  - `ScoreCopyResult`:
    - score: number
    - breakdown: ScoreBreakdown
    - recommendation: string[]
- Export function:
  - `scoreCopy(input: GenerateRequest): ScoreCopyResult`
- Rule-based scoring design:
  - Build normalized corpus from `appName`, `category`, and `screenshots.join(" ")` (lowercase)
  - Each dimension score range: 0..20
  - `cta`: count CTA keywords (e.g. download, get, start, try, now, free, join, unlock)
  - `benefit`: count benefit keywords (e.g. save, easy, fast, improve, boost, productivity, growth)
  - `clarity`: start from base score and penalize too-short/too-long copy and noisy punctuation
  - `numeric`: detect digits/percentages/stat-like patterns
  - `emotion`: count emotional words (e.g. amazing, love, powerful, effortless, confident)
  - Clamp all dimension scores to 0..20
  - Total `score` = rounded sum of 5 dimensions (0..100)
- Recommendation rules:
  - If a dimension < 12, push one specific actionable recommendation for that dimension
  - If no weak dimensions, add one maintenance recommendation

[CREATE] packages/scoring/src/index.ts
- Re-export `scoreCopy` and exported scoring types

[EDIT] apps/api/package.json
- Add dependency:
  - "@aso-copilot/scoring": "workspace:*"

[EDIT] apps/api/src/index.ts
- Import `scoreCopy` from `@aso-copilot/scoring`
- In `/generate` after `GenerateRequestSchema.safeParse` success:
  - call `const scoring = scoreCopy(parsed.data)`
  - set response `score: scoring.score`
  - set response `recommendation: scoring.recommendation`
- Keep `GenerateResponseSchema.parse(response)` enforcement

[EDIT] apps/api/test/index.spec.ts
- `generate success`:
  - assert `score` is number and in 0..100
  - assert `recommendation` is array and non-empty
- `generate validation error`:
  - keep screenshots length failure assertion

[RUN]
1) pnpm install (repo root)
2) pnpm -C apps/api test

Response format:
1) Changed file list
2) Final code for each changed file
3) Test result summary
```

IMPORTANT:
- Do NOT change GenerateResponse shape.
- Do NOT add new fields to API response.
- Do NOT remove existing fields.
- Only replace score and recommendation values using scoring result.
