# `free_default` Search and Entitlement Write Conditions

요청 사항:
1. 전체 프로젝트에서 `free_default` 검색
2. `free_default` entitlement를 insert/upsert 하는 함수 확인
3. 함수는 부분이 아닌 전체 본문으로 제시
4. free entitlement가 기록되는 조건 식별
5. 코드 수정은 하지 않음

---

## 1) Project-wide Search Result (`free_default`)

확인된 매치:
- `C:/Users/Administrator/aso-copilot/apps/api/src/db/migrations/0001_billing_usage.sql:68`
- `C:/Users/Administrator/aso-copilot/apps/api/src/repositories/entitlements.repo.ts:31`
- `C:/Users/Administrator/aso-copilot/apps/api/src/repositories/entitlements.repo.ts:41`
- `C:/Users/Administrator/aso-copilot/apps/api/src/routes/webhooks.polar.ts:217`
- `C:/Users/Administrator/aso-copilot/apps/api/test/index.spec.ts:201`
- `docs/`, `docs_result/` 내 결과 기록 문서에도 동일 문자열 존재

---

## 2) Function that Inserts/Upsets `free_default` Entitlements

### A. `insertFreeDefault` (insert)

파일:
- `C:/Users/Administrator/aso-copilot/apps/api/src/repositories/entitlements.repo.ts`

전체 함수:

```ts
async insertFreeDefault(uid: string): Promise<void> {
  const now = new Date().toISOString()
  await this.db
    .prepare(
      `INSERT OR IGNORE INTO user_entitlements
         (uid, plan_code, status, source_type, usage_limit_monthly, effective_from, version, updated_at)
       VALUES (?, 'free', 'active', 'free_default', 3, ?, 1, ?)`,
    )
    .bind(uid, now, now)
    .run()
}
```

### B. `upsertFromSubscription` (upsert)

파일:
- `C:/Users/Administrator/aso-copilot/apps/api/src/repositories/entitlements.repo.ts`

전체 함수:

```ts
async upsertFromSubscription(
  uid: string,
  planCode: string,
  sourceType: 'polar_subscription' | 'free_default',
  sourceRef: string | null,
  usageLimitMonthly: number | null,
): Promise<void> {
  const now = new Date().toISOString()
  await this.db
    .prepare(
      `INSERT INTO user_entitlements
         (uid, plan_code, status, source_type, source_ref, usage_limit_monthly, effective_from, version, updated_at)
       VALUES (?, ?, 'active', ?, ?, ?, ?, 1, ?)
       ON CONFLICT(uid) DO UPDATE SET
         plan_code            = excluded.plan_code,
         status               = excluded.status,
         source_type          = excluded.source_type,
         source_ref           = excluded.source_ref,
         usage_limit_monthly  = excluded.usage_limit_monthly,
         version              = version + 1,
         updated_at           = excluded.updated_at`,
    )
    .bind(uid, planCode, sourceType, sourceRef, usageLimitMonthly, now, now)
    .run()
}
```

---

## 3) Under What Condition Free Entitlement Is Written

### Case 1: Initial default entitlement creation

호출 위치:
- `C:/Users/Administrator/aso-copilot/apps/api/src/services/entitlement.service.ts:37`

조건:
- `getOrCreateEntitlement`에서 `entRepo.get(uid)` 결과가 `null`일 때
- `entRepo.insertFreeDefault(uid)` 호출
- 즉, 해당 `uid`의 entitlement row가 아직 없을 때 free 기본 row가 생성됨

### Case 2: Webhook recompute path writes free via upsert

호출 위치:
- `C:/Users/Administrator/aso-copilot/apps/api/src/routes/webhooks.polar.ts:217`

조건:
- 웹훅 처리 중 `if (active && planCode === 'pro')` 조건을 만족하지 않으면 else 분기 진입
- 아래 호출 실행:
  - `entRepo.upsertFromSubscription(uid, 'free', 'free_default', null, 3)`
- 실질적으로 free로 기록되는 대표 상황:
  - 구독이 active/trialing이 아닌 상태
  - planCode가 pro가 아닌 경우
  - product mapping 실패로 planCode가 free로 강제된 경우

---

## 4) Change Policy

이번 작업에서는 검색/분석만 수행했고 코드 수정은 하지 않았습니다.
