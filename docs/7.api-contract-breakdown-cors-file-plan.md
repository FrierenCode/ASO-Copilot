1. 수정: [packages/shared/src/generate.ts](/C:/Users/Administrator/aso-copilot/packages/shared/src/generate.ts)  
`GenerateResponseSchema`에 `breakdown` 객체를 추가(`cta`, `benefit`, `clarity`, `numeric`, `emotion`, `category` 모두 `z.number()`), 기존 `variants`/`score`/`recommendation` 필드는 유지. `GenerateResponse` 타입은 스키마 추론으로 자동 확장.

2. 확인(필요 시 수정): [packages/shared/src/index.ts](/C:/Users/Administrator/aso-copilot/packages/shared/src/index.ts)  
기존 barrel export 유지 확인(대부분 수정 없음). shared 타입 노출 경로가 그대로 유효한지 확인.

3. 수정: [apps/api/src/index.ts](/C:/Users/Administrator/aso-copilot/apps/api/src/index.ts)  
전역 CORS 미들웨어 추가:
- `Access-Control-Allow-Origin: *`
- `Access-Control-Allow-Methods: GET, POST, OPTIONS`
- `Access-Control-Allow-Headers: Content-Type`  
`OPTIONS` 핸들러 추가(204 반환).  
`/generate` 응답에 `scoreCopy` 결과의 `breakdown` 포함 후 `GenerateResponseSchema.parse(response)` 통과하도록 정렬.

4. 수정: [apps/api/test/index.spec.ts](/C:/Users/Administrator/aso-copilot/apps/api/test/index.spec.ts)  
성공 케이스에서 `breakdown` 존재 검증 추가, 각 필드(`cta`, `benefit`, `clarity`, `numeric`, `emotion`, `category`)가 숫자인지 검증. 기존 validation 실패 테스트는 유지.

5. 테스트 보강(필요 시 추가): [apps/api/test/index.spec.ts](/C:/Users/Administrator/aso-copilot/apps/api/test/index.spec.ts)  
CORS 동작 확인을 위해 `OPTIONS /generate` 또는 공통 라우트에 대해 204 및 CORS 헤더 검증 케이스 추가(기존 테스트 유지).

6. 자동 갱신 가능: [pnpm-lock.yaml](/C:/Users/Administrator/aso-copilot/pnpm-lock.yaml)  
스키마/테스트 변경만으로 보통 의존성 변화는 없지만, 설치/테스트 과정에서 lockfile 변경 시 반영.
