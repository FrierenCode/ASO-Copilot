# ASO-Copilot Production Architecture (Billing/Entitlements/Usage)

프로젝트 스택:
- Cloudflare Workers (Hono)
- Cloudflare D1
- Next.js (Pages)
- pnpm monorepo
- Polar payments (Webhook 기반)
- Cookie 기반 익명 사용자 식별 (`uid`, UUID)

목표:
- 익명 사용자 과금/권한/사용량을 서버에서 단일 진실 원천(Single Source of Truth)으로 관리
- 수평 확장 가능한 구조
- 클라이언트 변조에 안전한 서버 측 검증

---

## 1) D1 Database Schema (SQL)

```sql
PRAGMA foreign_keys = ON;

-- 1) Users (anonymous uid cookie is the identity key)
CREATE TABLE IF NOT EXISTS users (
  uid TEXT PRIMARY KEY CHECK(length(uid) = 36),
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  last_seen_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
);

-- 2) Plan catalog (single place for limits + future plans)
CREATE TABLE IF NOT EXISTS plan_catalog (
  plan_code TEXT PRIMARY KEY CHECK(plan_code IN ('free','pro','premium')),
  monthly_generation_limit INTEGER,               -- NULL = unlimited
  is_enabled INTEGER NOT NULL DEFAULT 1 CHECK(is_enabled IN (0,1)),
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
);

INSERT OR IGNORE INTO plan_catalog (plan_code, monthly_generation_limit, is_enabled)
VALUES
  ('free', 3, 1),
  ('pro', NULL, 1),
  ('premium', NULL, 0); -- reserved for future

-- 3) Polar product/price -> internal plan mapping
CREATE TABLE IF NOT EXISTS polar_product_plan_map (
  polar_product_id TEXT PRIMARY KEY,
  plan_code TEXT NOT NULL REFERENCES plan_catalog(plan_code),
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
);

-- 4) Billing customers (provider identity linked to uid)
CREATE TABLE IF NOT EXISTS billing_customers (
  polar_customer_id TEXT PRIMARY KEY,
  uid TEXT NOT NULL REFERENCES users(uid) ON DELETE CASCADE,
  email TEXT,
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
);
CREATE INDEX IF NOT EXISTS idx_billing_customers_uid
  ON billing_customers(uid);

-- 5) Current subscription state per Polar subscription
CREATE TABLE IF NOT EXISTS billing_subscriptions (
  polar_subscription_id TEXT PRIMARY KEY,
  polar_customer_id TEXT NOT NULL REFERENCES billing_customers(polar_customer_id) ON DELETE CASCADE,
  uid TEXT NOT NULL REFERENCES users(uid) ON DELETE CASCADE,
  plan_code TEXT NOT NULL REFERENCES plan_catalog(plan_code),
  status TEXT NOT NULL,
  cancel_at_period_end INTEGER NOT NULL DEFAULT 0 CHECK(cancel_at_period_end IN (0,1)),
  current_period_start TEXT,
  current_period_end TEXT,
  source_updated_at TEXT NOT NULL,               -- from provider object/event timestamp
  raw_json TEXT NOT NULL,                        -- audit/debug
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
);
CREATE INDEX IF NOT EXISTS idx_subscriptions_uid_status_period
  ON billing_subscriptions(uid, status, current_period_end);
CREATE INDEX IF NOT EXISTS idx_subscriptions_source_updated
  ON billing_subscriptions(source_updated_at);

-- 6) Single source of truth for request-time authorization
CREATE TABLE IF NOT EXISTS user_entitlements (
  uid TEXT PRIMARY KEY REFERENCES users(uid) ON DELETE CASCADE,
  plan_code TEXT NOT NULL REFERENCES plan_catalog(plan_code),
  status TEXT NOT NULL CHECK(status IN ('active','inactive')),
  source_type TEXT NOT NULL CHECK(source_type IN ('free_default','polar_subscription')),
  source_ref TEXT,                               -- e.g., polar_subscription_id
  usage_limit_monthly INTEGER,                   -- NULL = unlimited
  effective_from TEXT NOT NULL,
  effective_until TEXT,
  version INTEGER NOT NULL DEFAULT 1,
  updated_at TEXT NOT NULL
);

-- 7) Monthly usage counter (fast quota check path)
CREATE TABLE IF NOT EXISTS usage_monthly (
  uid TEXT NOT NULL REFERENCES users(uid) ON DELETE CASCADE,
  period_start TEXT NOT NULL,                    -- UTC month anchor: YYYY-MM-01
  used_count INTEGER NOT NULL DEFAULT 0 CHECK(used_count >= 0),
  updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  PRIMARY KEY (uid, period_start)
);
CREATE INDEX IF NOT EXISTS idx_usage_monthly_period
  ON usage_monthly(period_start);

-- 8) Request-level usage audit + idempotency
CREATE TABLE IF NOT EXISTS generation_requests (
  uid TEXT NOT NULL REFERENCES users(uid) ON DELETE CASCADE,
  request_id TEXT NOT NULL,                      -- client/server UUID for idempotency
  period_start TEXT NOT NULL,
  plan_code TEXT NOT NULL REFERENCES plan_catalog(plan_code),
  decision TEXT NOT NULL CHECK(decision IN ('allowed','rejected_limit','failed')),
  counted INTEGER NOT NULL CHECK(counted IN (0,1)),
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  PRIMARY KEY (uid, request_id)
);
CREATE INDEX IF NOT EXISTS idx_generation_requests_uid_period
  ON generation_requests(uid, period_start);

-- 9) Webhook inbox for dedupe/replay-safe processing
CREATE TABLE IF NOT EXISTS webhook_inbox (
  provider TEXT NOT NULL CHECK(provider IN ('polar')),
  webhook_id TEXT NOT NULL,                      -- from webhook-id header (Standard Webhooks)
  provider_event_id TEXT,                        -- if payload includes stable event id
  event_type TEXT NOT NULL,
  event_created_at TEXT,
  signature_valid INTEGER NOT NULL CHECK(signature_valid IN (0,1)),
  payload TEXT NOT NULL,
  status TEXT NOT NULL CHECK(status IN ('received','processing','processed','ignored','failed')),
  attempt_count INTEGER NOT NULL DEFAULT 0,
  first_received_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  last_received_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  processed_at TEXT,
  last_error TEXT,
  PRIMARY KEY (provider, webhook_id)
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_webhook_provider_event
  ON webhook_inbox(provider, provider_event_id)
  WHERE provider_event_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_webhook_status_received
  ON webhook_inbox(status, first_received_at);
```

---

## 2) Worker Folder Structure (apps/api)

```txt
apps/api/src/
  index.ts
  env.ts
  db/
    client.ts
    migrations/
      0001_billing_usage.sql
    repositories/
      users.repo.ts
      subscriptions.repo.ts
      entitlements.repo.ts
      usage.repo.ts
      webhook.repo.ts
  middleware/
    cors.ts
    uidCookie.ts
    requestId.ts
  routes/
    health.ts
    generate.ts
    entitlements.ts
    webhooks.polar.ts
  billing/
    polar/
      verifySignature.ts
      eventParser.ts
      handlers/
        subscriptionCreated.ts
        subscriptionUpdated.ts
        subscriptionCanceled.ts
        subscriptionRevoked.ts
        orderCreated.ts
  services/
    entitlement.service.ts
    usage-gate.service.ts
    webhook-processor.service.ts
    generate.service.ts
  jobs/
    replay-failed-webhooks.ts
  types/
    billing.ts
    api.ts
```

---

## 3) Webhook Flow Diagram (Text)

1. `Polar -> POST /webhooks/polar` (raw body + Standard Webhooks signature headers)
2. Worker가 raw body로 signature + timestamp tolerance 검증
3. `webhook_inbox`에 `(provider, webhook_id)` 기준 upsert
4. 이미 처리된 중복이면 즉시 `200`
5. 신규면 `status=processing`
6. 이벤트 타입별 핸들러 실행:
   - customer/subscription/order 정보를 `billing_customers`, `billing_subscriptions`에 upsert
   - `source_updated_at` 비교로 out-of-order 이벤트 무시
7. 같은 트랜잭션에서 `user_entitlements` 재계산/갱신
8. 성공 시 `status=processed`, 실패 시 `status=failed + last_error`
9. 항상 빠르게 `2xx` 반환 (재시도 폭주 방지), 실패 건은 스케줄러 재처리

---

## 4) Entitlements API Contract

엔드포인트:
- `GET /v1/entitlements`

인증/식별:
- `uid`는 서버가 서명 검증한 `HttpOnly` 쿠키에서만 추출

성공 응답 (`200`):

```json
{
  "uid": "uuid",
  "plan": "free",
  "status": "active",
  "limits": {
    "monthlyGenerations": 3,
    "usedThisMonth": 1,
    "remainingThisMonth": 2,
    "periodStart": "2026-03-01T00:00:00Z",
    "periodEnd": "2026-04-01T00:00:00Z"
  },
  "source": {
    "type": "free_default",
    "ref": null,
    "version": 12,
    "updatedAt": "2026-03-02T10:00:00Z"
  }
}
```

정책:
- `pro`(무제한)이면 `monthlyGenerations`, `remainingThisMonth`는 `null`
- 응답은 `user_entitlements` + `usage_monthly` 기반으로 서버에서만 계산
- `Cache-Control: no-store`

---

## 5) Server-side Usage Enforcement Logic (/generate 통합)

`POST /generate` 처리 순서:

1. 서명 검증된 `uid` 쿠키 파싱 (없으면 생성 후 설정)
2. 사용자 최초 접근 시:
   - `users` upsert
   - `user_entitlements` 기본값 `free(3/month)` 생성
3. 현재 entitlement를 `user_entitlements`에서 조회 (클라이언트 입력 무시)
4. `pro` 무제한이면 허용 후 감사 로그(`generation_requests`) 기록
5. `free` 제한 플랜이면 월 카운터 원자적 증가 시도:

```sql
INSERT INTO usage_monthly (uid, period_start, used_count, updated_at)
VALUES (?1, ?2, 1, ?3)
ON CONFLICT(uid, period_start) DO UPDATE SET
  used_count = used_count + 1,
  updated_at = excluded.updated_at
WHERE used_count < ?4;
```

6. 변경 row 수가 `0`이면 quota 초과 (`429`)
7. `generation_requests(uid, request_id)` unique로 idempotency 보장
8. 게이트 통과 시에만 실제 생성 로직 실행

---

## 6) Edge Cases

- `cancel_at_period_end=true`:
  - 현재 기간 종료 전까지 `pro` 유지
  - 기간 만료 또는 revoke 이벤트 시 `free`로 강등
- 즉시 취소/환불/revoked:
  - 웹훅 처리 트랜잭션에서 즉시 entitlement 강등
- `past_due`:
  - 상태는 저장하되 정책적으로 grace 허용 여부를 명시 (권장: 기간 종료까지 유지 후 만료 시 강등)
- 웹훅 중복 전달:
  - `webhook_id` primary key로 dedupe
- 웹훅 순서 역전(out-of-order):
  - `source_updated_at`가 더 오래된 이벤트는 무시
- uid 매핑 누락:
  - `failed`로 적재하고 재시도 잡에서 복구
- `/generate` 클라이언트 재시도:
  - `request_id` idempotency로 중복 과금/중복 카운트 방지

---

## 7) Security Considerations

- Polar 웹훅 서명 검증은 반드시 **raw body** 기준으로 수행
- timestamp tolerance 검사로 replay 공격 완화
- 웹훅 idempotency key(`webhook-id`) 영속 저장
- 쿠키 설정:
  - `HttpOnly`, `Secure`, `SameSite=Lax`
  - 값 서명(HMAC)으로 변조 탐지
- 플랜/권한/사용량 검증은 100% 서버 측에서만 수행
- 프로덕션 CORS는 화이트리스트 origin만 허용 (`*` 금지)
- SQL은 prepared statement만 사용
- `/generate` 및 `/webhooks/polar`에 rate limit/WAF 적용
- 웹훅 secret은 Wrangler secret으로 관리 + rotation 지원
- webhook endpoint는 공개하되 앱 사용자 인증 미들웨어와 분리

---

## 8) Future Extensibility Notes

- `premium` 플랜:
  - `plan_catalog`에 이미 존재
  - `polar_product_plan_map`만 추가하면 활성화 가능
- 기능별 권한(예: 고급 리포트, export):
  - `feature_entitlements` 테이블 추가로 확장 가능
- 월간 외 과금 주기(일/연/좌석/미터링):
  - usage period를 일반화한 테이블로 확장
- 웹훅 burst 대응:
  - Cloudflare Queues로 비동기 ingest + processor worker 분리
- 익명 uid -> 로그인 계정 전환:
  - identity linking table 추가 후 usage/entitlement 이관
- 정합성 보정:
  - 주기적 reconciliation job으로 Polar API와 D1 상태 비교/복구

---

## 운영 원칙 요약

- Single Source of Truth: `user_entitlements`
- Quota Source of Truth: `usage_monthly`
- Idempotency Source of Truth: `webhook_inbox`, `generation_requests`
- 모든 판단은 서버에서만 수행하며, 클라이언트는 표시 전용
