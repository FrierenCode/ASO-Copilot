v2.2 업그레이드 반영 완료했습니다. API 응답 shape는 변경하지 않았습니다.

변경 파일:
- [scoreCopy.ts](/C:/Users/Administrator/aso-copilot/packages/scoring/src/scoreCopy.ts)
- [index.spec.ts](/C:/Users/Administrator/aso-copilot/apps/api/test/index.spec.ts)

적용 내용:
- `benefit`를 ratio 기반으로 변경:
  - `benefitRatio = benefitWordCount / totalWordCount`
  - `benefit = clamp(Math.round(benefitRatio * 60), 0, 20)`
- `category` 차원 추가(0..25):
  - `CATEGORY_MAP` 도입
  - category 키워드 매칭 수 기반 `alignmentRatio`
  - `category = clamp(Math.round(alignmentRatio * 100), 0, 25)`
- `ScoreBreakdown`에 `category` 필드 추가.
- 가중치 점수 분배를 v2.2로 변경:
  - CTA 20, Benefit 20, Clarity 15, Numeric 10, Emotion 10, Category 25
  - 총점은 `0..100`으로 반올림/클램프.
- 숫자 감지는 기존 v2.1 방식 유지:
  - screenshot filename 노이즈 제외 후 numeric 계산(결정적 로직).

테스트 업데이트:
- filename-only 입력에서 numeric 과대평가 방지 검증 유지.
- category mismatch 시 점수가 낮아지는 비교 테스트 추가.
- 점수 `0..100` 범위 검증 추가/강화.

검증:
- 실행: `pnpm -C apps/api test`
- 결과: `5 passed` (전체 통과)
